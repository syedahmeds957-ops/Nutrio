diff --git a/node_modules/expo-modules-jsi/apple/Package.swift b/node_modules/expo-modules-jsi/apple/Package.swift
index 5798eaf..8e33dbd 100644
--- a/node_modules/expo-modules-jsi/apple/Package.swift
+++ b/node_modules/expo-modules-jsi/apple/Package.swift
@@ -91,6 +91,18 @@ let package = Package(
         .enableUpcomingFeature("NonisolatedNonsendingByDefault"),
         // https://github.com/swiftlang/swift-evolution/blob/main/proposals/0470-isolated-conformances.md
         .enableUpcomingFeature("InferIsolatedConformances"),
+        // Swift 6 features needed while building in Swift 5 mode (Xcode < 27).
+        .enableUpcomingFeature("BareSlashRegexLiterals"),
+        .enableUpcomingFeature("IsolatedDefaultValues"),
+        .enableUpcomingFeature("GlobalActorIsolatedTypesUsability"),
+        .enableUpcomingFeature("InferSendableFromCaptures"),
+        .enableUpcomingFeature("DisableOutwardActorInference"),
+        .enableUpcomingFeature("ConciseMagicFile"),
+        .enableUpcomingFeature("ForwardTrailingClosures"),
+        .enableUpcomingFeature("ImportObjcForwardDeclarations"),
+        .enableUpcomingFeature("DeprecateApplicationMain"),
+        .enableUpcomingFeature("ImplicitOpenExistentials"),
+        .enableUpcomingFeature("DynamicActorIsolation"),
 
         .unsafeFlags([
           "-enable-library-evolution",
@@ -140,7 +152,7 @@ let package = Package(
       dependencies: testFrameworks.dependencies,
     ),
   ] + testFrameworks.binaryTargets,
-  swiftLanguageModes: [.v6],
+  swiftLanguageModes: [.v5],
   cxxLanguageStandard: .cxx20
 )
 
diff --git a/node_modules/expo-modules-jsi/apple/Sources/ExpoModulesJSI-Cxx/include/RuntimeScheduler.h b/node_modules/expo-modules-jsi/apple/Sources/ExpoModulesJSI-Cxx/include/RuntimeScheduler.h
index 6115e87..fab634b 100644
--- a/node_modules/expo-modules-jsi/apple/Sources/ExpoModulesJSI-Cxx/include/RuntimeScheduler.h
+++ b/node_modules/expo-modules-jsi/apple/Sources/ExpoModulesJSI-Cxx/include/RuntimeScheduler.h
@@ -5,6 +5,13 @@
 #include <atomic>
 #include <swift/bridging>
 
+// Xcode < 27 (Apple clang < 1800) rejects ownership annotations on constructors.
+#if defined(__apple_build_version__) && __apple_build_version__ < 18000000
+#define EXPO_CTOR_RETURNS_RETAINED
+#else
+#define EXPO_CTOR_RETURNS_RETAINED SWIFT_RETURNS_RETAINED
+#endif
+
 namespace expo {
 
 /**
@@ -50,7 +57,7 @@ public:
    `scheduleTask` dispatches through `fn`, which the host implements against
    the real react::RuntimeScheduler.
    */
-  SWIFT_RETURNS_RETAINED RuntimeScheduler(void *scheduler, ScheduleFn fn) noexcept
+  EXPO_CTOR_RETURNS_RETAINED RuntimeScheduler(void *scheduler, ScheduleFn fn) noexcept
       : nativeScheduler(scheduler), scheduleFn(fn) {}
 
   /**
@@ -58,7 +65,7 @@ public:
    caller's thread — intended for standalone runtimes (e.g. tests) that have
    no React scheduler.
    */
-  SWIFT_RETURNS_RETAINED RuntimeScheduler() {}
+  EXPO_CTOR_RETURNS_RETAINED RuntimeScheduler() {}
 
   RuntimeScheduler(const RuntimeScheduler &) = delete;
 
